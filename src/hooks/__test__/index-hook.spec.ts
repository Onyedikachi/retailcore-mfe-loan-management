import useDebounce from '../useDebounce';

describe('useDebounce', () => {
  describe('useDebounce', () => {
    it('should do something', () => {
      //write here
    });
  });

  // ... other tests for additional functions exported from useDebounce
});
