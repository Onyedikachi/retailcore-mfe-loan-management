import { Colors } from '@app/constants/colors';

export const RadioTheme = {
   styleOverrides: {
      root: {
         padding: '9px 4px',
         '& .MuiSvgIcon-root': {
            fontSize: '17px',
            fill: Colors.Primary,
         },
      },
   },
};
